version: 4.0.69
steps:
  - prompt: Click on "Pricing" in the header.
    commands:
      - command: hover-text
        text: Pricing
        description: link in the header
        action: click
  - prompt: Assert that the pricing options are displayed.
    commands:
      - command: assert
        expect: the pricing options are displayed
  - prompt: >-
      Check that each pricing plan has detailed information.  # Use the content
      of the file from the environment
    commands:
      - command: assert
        expect: each pricing plan has detailed information
